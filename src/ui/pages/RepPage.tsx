import { Card } from '../components/Card'

export default function RepPage() {
  return (
    <div className="page-stack">
      <Card>
        <h2>واجهة المندوب</h2>
        <p className="muted">هذا القسم جاهز لربط customers / sales reps / rep analytics على الـ views الرسمية.</p>
      </Card>
    </div>
  )
}
